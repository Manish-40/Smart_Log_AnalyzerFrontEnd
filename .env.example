# Base URL of your deployed backend (no trailing slash), e.g.:
# VITE_API_URL=https://log-analyzer-server.vercel.app
# Leave empty for local dev (Vite proxy handles /api -> localhost:8080)
VITE_API_URL=
